# Firebase - coloque o caminho do serviceAccountKey.json
FIREBASE_KEY=serviceAccountKey.json
FIREBASE_PROJECT=seu-projeto-firebase

# Se não tiver Firebase, a API funciona com memória local (sem persistência)
